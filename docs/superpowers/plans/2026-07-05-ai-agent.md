# AI Agent — 对话操作看板 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** AI 助手升级为看板 agent——用户对话中给出指令,AI 返回操作建议(actions),用户确认后执行(增删改需求/成员/迭代)。

**Architecture:** prompt 工程返回 actions JSON;前端显示建议卡片+确认;后端 /api/ai/execute/ 调 ORM 执行。

**Tech Stack:** Django+DRF、智谱 GLM glm-5.2(anthropic 端点)、React+shadcn。

## Global Constraints

- 所有写操作必须用户点确认(AI 只建议)
- 沿用现有模式:NoCSRFSessionAuthentication、trailing-slash URL、anthropic 端点
- macOS 执行(venv backend/.venv, Python 3.14);前端 npm --prefix
- commit 带 Co-Authored-By: Claude <noreply@anthropic.com>

---

### Task 1: ai_actions.py 执行器(纯函数)

**Files:**
- Create: `backend/apps/core/ai_actions.py`
- Create: `backend/apps/core/tests/test_ai_actions.py`

**Interfaces:**
- Produces: `execute_action(action) -> {success, message, affected}`

- [ ] **Step 1: 写测试**

Create `backend/apps/core/tests/test_ai_actions.py`:
```python
import pytest
from apps.core.models import Member, Sprint, Requirement
from apps.core.ai_actions import execute_action
from datetime import date

@pytest.fixture
def setup_data():
    m = Member.objects.create(name="张三", week_capacity=40)
    s = Sprint.objects.create(name="S1", start_date=date(2026,7,6), end_date=date(2026,7,20))
    r = Requirement.objects.create(title="登录接口", assignee=m, assigned_sprint=s, status="backlog")
    return m, s, r

@pytest.mark.django_db
def test_update_requirement_by_title(setup_data):
    m, s, r = setup_data
    result = execute_action({
        "type": "update_requirement",
        "match": {"title": "登录接口"},
        "fields": {"status": "in_progress", "assignee": "张三"}
    })
    assert result["success"] is True
    r.refresh_from_db()
    assert r.status == "in_progress"

@pytest.mark.django_db
def test_update_requirement_not_found():
    result = execute_action({
        "type": "update_requirement",
        "match": {"title": "不存在"},
        "fields": {"status": "done"}
    })
    assert result["success"] is False
    assert "不存在" in result["message"]

@pytest.mark.django_db
def test_create_requirement(setup_data):
    m, s, r = setup_data
    result = execute_action({
        "type": "create_requirement",
        "params": {"title": "支付接口", "assignee": "张三", "assigned_sprint": "S1"}
    })
    assert result["success"] is True
    assert Requirement.objects.filter(title="支付接口").exists()

@pytest.mark.django_db
def test_delete_requirement_by_title(setup_data):
    result = execute_action({
        "type": "delete_requirement",
        "match": {"title": "登录接口"}
    })
    assert result["success"] is True
    assert not Requirement.objects.filter(title="登录接口").exists()

@pytest.mark.django_db
def test_update_member(setup_data):
    result = execute_action({
        "type": "update_member",
        "match": {"name": "张三"},
        "fields": {"week_capacity": 35}
    })
    assert result["success"] is True

@pytest.mark.django_db
def test_unknown_action():
    result = execute_action({"type": "fly_to_moon"})
    assert result["success"] is False
    assert "不支持" in result["message"]
```

- [ ] **Step 2: 跑测试确认失败**

Run: `cd backend && .venv/bin/pytest apps/core/tests/test_ai_actions.py -v`
Expected: FAIL(模块不存在)

- [ ] **Step 3: 实现 ai_actions.py**

Create `backend/apps/core/ai_actions.py`:
```python
from .models import Member, Sprint, Requirement

def _find_requirement(match):
    if "id" in match:
        return Requirement.objects.filter(id=match["id"]).first()
    if "title" in match:
        return Requirement.objects.filter(title__icontains=match["title"]).first()
    return None

def _resolve_member(name_or_id):
    if name_or_id is None:
        return None
    if isinstance(name_or_id, int):
        return Member.objects.filter(id=name_or_id).first()
    return Member.objects.filter(name=name_or_id).first()

def _resolve_sprint(name_or_id):
    if name_or_id is None:
        return None
    if isinstance(name_or_id, int):
        return Sprint.objects.filter(id=name_or_id).first()
    return Sprint.objects.filter(name=name_or_id).first()

def execute_action(action):
    """执行一个 AI 建议的操作。返回 {success, message, affected}。"""
    try:
        atype = action.get("type")
        if atype == "update_requirement":
            r = _find_requirement(action.get("match", {}))
            if not r:
                return {"success": False, "message": f"找不到需求「{action.get('match', {}).get('title', '')}」"}
            fields = action.get("fields", {})
            if "status" in fields:
                r.status = fields["status"]
            if "priority" in fields:
                r.priority = fields["priority"]
            if "module" in fields:
                r.module = fields["module"]
            if "est_effort" in fields:
                r.est_effort = fields["est_effort"]
            if "progress" in fields:
                r.progress = fields["progress"]
            if "assignee" in fields:
                m = _resolve_member(fields["assignee"])
                if m:
                    r.assignee = m
            if "assigned_sprint" in fields:
                s = _resolve_sprint(fields["assigned_sprint"])
                if s:
                    r.assigned_sprint = s
            r.save()
            return {"success": True, "message": f"已更新「{r.title}」", "affected": {"id": r.id, "title": r.title}}

        elif atype == "create_requirement":
            params = action.get("params", {})
            r = Requirement(title=params.get("title", "未命名"), status=params.get("status", "backlog"), priority=params.get("priority", "P1"))
            if params.get("assignee"):
                m = _resolve_member(params["assignee"])
                if m: r.assignee = m
            if params.get("assigned_sprint"):
                s = _resolve_sprint(params["assigned_sprint"])
                if s: r.assigned_sprint = s
            if params.get("est_effort"):
                r.est_effort = params["est_effort"]
            if params.get("module"):
                r.module = params["module"]
            r.save()
            return {"success": True, "message": f"已创建「{r.title}」", "affected": {"id": r.id, "title": r.title}}

        elif atype == "delete_requirement":
            r = _find_requirement(action.get("match", {}))
            if not r:
                return {"success": False, "message": f"找不到需求「{action.get('match', {}).get('title', '')}」"}
            title = r.title
            r.delete()
            return {"success": True, "message": f"已删除「{title}」", "affected": {"title": title}}

        elif atype == "update_member":
            match = action.get("match", {})
            m = _resolve_member(match.get("name") or match.get("id"))
            if not m:
                return {"success": False, "message": f"找不到成员「{match}」"}
            fields = action.get("fields", {})
            if "week_capacity" in fields:
                m.week_capacity = fields["week_capacity"]
            if "modules" in fields:
                m.modules = fields["modules"]
            if "active" in fields:
                m.active = fields["active"]
            m.save()
            return {"success": True, "message": f"已更新成员「{m.name}」", "affected": {"id": m.id, "name": m.name}}

        elif atype == "update_sprint":
            match = action.get("match", {})
            s = _resolve_sprint(match.get("name") or match.get("id"))
            if not s:
                return {"success": False, "message": f"找不到迭代「{match}」"}
            fields = action.get("fields", {})
            if "name" in fields:
                s.name = fields["name"]
            if "start_date" in fields:
                s.start_date = fields["start_date"]
            if "end_date" in fields:
                s.end_date = fields["end_date"]
            if "is_active" in fields:
                s.is_active = fields["is_active"]
            s.save()
            return {"success": True, "message": f"已更新迭代「{s.name}」", "affected": {"id": s.id, "name": s.name}}

        else:
            return {"success": False, "message": f"不支持的操作类型: {atype}"}
    except Exception as e:
        return {"success": False, "message": f"执行失败: {str(e)}"}
```

- [ ] **Step 4: 跑测试通过**

Run: `cd backend && .venv/bin/pytest apps/core/tests/test_ai_actions.py -v`
Expected: 6 passed

- [ ] **Step 5: Commit**

```bash
git add backend/apps/core/ai_actions.py backend/apps/core/tests/test_ai_actions.py
git commit -m "feat(core): ai_actions execute_action — ORM CRUD from AI suggestions"
```

---

### Task 2: ai_execute 端点 + ai_chat 返回 actions

**Files:**
- Modify: `backend/apps/core/views.py`(加 ai_execute + ai_chat 返回 actions)
- Modify: `backend/apps/core/urls.py`(加 execute 端点)
- Modify: `backend/apps/core/ai.py`(prompt 加操作定义 + parse_actions)
- Create: `backend/apps/core/tests/test_ai_execute.py`

**Interfaces:**
- Consumes: Task 1(execute_action)
- Produces: `POST /api/ai/execute/` → `{success, message}`;`POST /api/ai/chat/` 返回新增 `actions`

- [ ] **Step 1: ai.py 加 parse_actions + prompt 升级**

在 `backend/apps/core/ai.py` 加:
```python
def parse_actions(text):
    """从 GLM 回复提取 actions JSON(与 drafts 同级,不同标记)"""
    m = re.search(r"```actions\s*(\[.*?\])\s*```", text, re.DOTALL)
    if not m:
        return []
    try:
        return json.loads(m.group(1))
    except json.JSONDecodeError:
        return []

def strip_actions_block(text):
    return re.sub(r"```actions\s*\[.*?\]\s*```", "", text, flags=re.DOTALL).strip()
```

`build_system_prompt` 末尾追加:
```
你可以帮用户操作看板数据。用户说自然语言指令,你理解后在回复末尾用 ```actions 返回操作建议。
可用操作:
- update_requirement: {"type":"update_requirement","match":{"title":"xxx"},"fields":{"status":"in_progress","assignee":"张三","priority":"P0"}}
- create_requirement: {"type":"create_requirement","params":{"title":"xxx","assignee":"张三","assigned_sprint":"S1"}}
- delete_requirement: {"type":"delete_requirement","match":{"title":"xxx"}}
- update_member: {"type":"update_member","match":{"name":"张三"},"fields":{"week_capacity":35}}
每个 action 加 "description":"人话描述"。
返回的是建议(未执行),用户确认后才执行。
```

- [ ] **Step 2: views.py 加 ai_execute + ai_chat 返回 actions**

```python
from .ai_actions import execute_action

@api_view(["POST"])
def ai_execute(request):
    action = request.data
    result = execute_action(action)
    return Response(result)

# ai_chat 修改:parse_actions + 返回
# 在现有 ai_chat 里:
from .ai import parse_actions, strip_actions_block
# reply 处理改为:
raw_reply = chat_with_glm(messages)
drafts = parse_drafts(raw_reply)
actions = parse_actions(raw_reply)
reply_text = strip_json_block(raw_reply)
reply_text = strip_actions_block(reply_text)
return Response({"reply": reply_text, "drafts": drafts, "actions": actions})
```

urls.py 加:
```python
path("ai/execute/", ai_execute),
```

- [ ] **Step 3: 写测试**

Create `backend/apps/core/tests/test_ai_execute.py`:
```python
import pytest
from django.contrib.auth.models import User
from rest_framework.test import APIClient

@pytest.fixture
def client():
    User.objects.create_user("pl", password="pw")
    c = APIClient(); c.login(username="pl", password="pw")
    return c

@pytest.mark.django_db
def test_ai_execute_update(client):
    from apps.core.models import Member, Requirement
    m = Member.objects.create(name="张三")
    r = Requirement.objects.create(title="测试需求", assignee=m)
    resp = client.post("/api/ai/execute/", {"type": "update_requirement", "match": {"title": "测试需求"}, "fields": {"status": "done"}}, format="json")
    assert resp.status_code == 200
    assert resp.data["success"] is True
    r.refresh_from_db()
    assert r.status == "done"

@pytest.mark.django_db
def test_ai_execute_not_found(client):
    resp = client.post("/api/ai/execute/", {"type": "update_requirement", "match": {"title": "不存在"}, "fields": {}}, format="json")
    assert resp.status_code == 200
    assert resp.data["success"] is False
```

- [ ] **Step 4: 跑测试**

Run: `cd backend && .venv/bin/pytest apps/core/tests/test_ai_execute.py apps/core/tests/test_ai_endpoint.py -v`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add backend/apps/core/views.py backend/apps/core/urls.py backend/apps/core/ai.py backend/apps/core/tests/test_ai_execute.py
git commit -m "feat(ai): ai_execute endpoint + chat returns actions suggestions"
```

---

### Task 3: 前端操作建议卡片 + 确认执行

**Files:**
- Modify: `frontend/src/types.ts`(Action 类型)
- Modify: `frontend/src/api.ts`(aiExecute)
- Modify: `frontend/src/components/AssistantWidget.tsx`(actions 卡片 + 确认)

- [ ] **Step 1: types.ts 加 Action**

```typescript
export interface AIAction {
  type: string
  match?: Record<string, any>
  fields?: Record<string, any>
  params?: Record<string, any>
  description?: string
}
```

- [ ] **Step 2: api.ts 加 aiExecute**

```typescript
aiExecute: (action: AIAction) =>
  http.post<{success: boolean; message: string}>('/api/ai/execute/', action).then(r => r.data),
```

- [ ] **Step 3: AssistantWidget 加 actions 显示 + 确认**

- `aiChat` 返回类型加 `actions: AIAction[]`
- state `pendingActions: AIAction[][]`(多轮多组)
- 收到 actions 时显示操作建议卡片
- 用户点「确认执行」→ `api.aiExecute(action)` → 结果反馈到对话
- 点「取消」→ 移除

关键渲染代码:
```tsx
// state
const [actionGroups, setActionGroups] = useState<AIAction[][]>([])

// send 里:
if (data.actions?.length) setActionGroups(prev => [...prev, data.actions])

// 执行
const executeAction = async (groupIdx: number, actionIdx: number) => {
  const action = actionGroups[groupIdx][actionIdx]
  try {
    const result = await api.aiExecute(action)
    setMessages(prev => [...prev, { role: 'assistant', content: result.success ? `✅ ${result.message}` : `❌ ${result.message}` }])
    // 从待确认列表移除
    setActionGroups(prev => prev.map((g, gi) => gi === groupIdx ? g.filter((_, ai) => ai !== actionIdx) : g).filter(g => g.length > 0))
  } catch { setError('执行失败') }
}

// 渲染
{actionGroups.map((group, gi) =>
  group.map((action, ai) => (
    <Card key={`${gi}-${ai}`} className="p-2 bg-blue-50 border-blue-200">
      <div className="text-sm">🔧 {action.description || JSON.stringify(action)}</div>
      <div className="flex gap-2 mt-1">
        <Button size="sm" onClick={() => executeAction(gi, ai)}>确认执行</Button>
        <Button size="sm" variant="outline" onClick={() => setActionGroups(prev => prev.map((g, gi2) => gi2 === gi ? g.filter((_, ai2) => ai2 !== ai) : g).filter(g => g.length > 0))}>取消</Button>
      </div>
    </Card>
  ))
)}
```

- [ ] **Step 4: tsc + build**

Run: `cd frontend && npx tsc --noEmit && npm run build`

- [ ] **Step 5: Commit**

```bash
git add frontend/src/types.ts frontend/src/api.ts frontend/src/components/AssistantWidget.tsx
git commit -m "feat(frontend): AI action suggestion cards + confirm-execute in AssistantWidget"
```

---

## Self-Review

- Task 1:execute_action 纯函数(update/create/delete/member/sprint)✓ spec §5
- Task 2:端点 /ai/execute/ + chat 返回 actions + prompt ✓ spec §3、§5
- Task 3:前端操作建议卡片 + 确认执行 ✓ spec §6
- 占位符:无(每步完整代码)
- 类型一致:execute_action(Task1 定,Task2 用);AIAction(Task3 定,前端用)✓

## 执行选择

Plan saved to `docs/superpowers/plans/2026-07-05-ai-agent.md`。
