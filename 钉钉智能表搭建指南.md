# 钉钉智能表 — 需求 & 人力看板搭建指南

> 适用:PL 个人自用 · Win10 + iPhone · 已在用钉钉
> 照着做,约 15 分钟搭好。

---

## 为什么选它

- **已在用钉钉** → 零迁移、不装新软件
- **国内 Win+iOS 原生客户端**,实时同步,iOS 推送稳(海外工具如 Notion 在公司 Win10 上常因无 VPN 直接出局)
- 一张智能表同时出「任务进度看板 + 人力负载表」,**透视表视图(2026.3 新增)是同类里唯一能按人汇总任务数 + 工时的**
- 免费版 2 万行/表,个人几百上千条需求用不完
- ⚠️ **坚持只用「智能表」一条主线**,别碰钉钉项目(Teambition)和待办——功能重叠,新手容易混

---

## 第一步:建表 + 字段

钉钉 → 底部「文档」→ 新建「智能表 / AI表格」→ 命名 `需求&人力看板`。

字段清单(就这套,别加多余的):

| 字段 | 类型 | 值 / 说明 |
|---|---|---|
| 需求标题 | 文本 | |
| 状态 | 单选 | 待评审 / 排期中 / 开发中 / 测试中 / 已上线(**按此顺序**,看板列序才对) |
| 优先级 | 单选 | P0 / P1 / P2 |
| 负责人 | 文本 | 填名字即可。个人自用不绑组织架构,直接文本,看板/透视都能按它分组 |
| 负责模块 | 多选 | 后端 / 前端 / 数据 / 测试 … |
| 进度 | 数字 | % |
| 预计工时 | 数字 | 单位:人天 |
| 实际工时 | 数字 | 单位:人天 |
| 备注 | 文本 | |

---

## 第二步:任务进度看板

视图栏「+」→ 新建「**看板视图**」→ 命名 `任务进度` → 分组依据选「状态」→ 得 5 列进度看板。
拖拽卡片到别的列 = 自动改状态值。**设为默认/置顶视图**。

(可选)再建一个「甘特视图」按日期看排期节奏,一个「表格视图」做明细录入。

---

## 第三步:人力情况看板(PL 核心 · 双视图)

这块最容易踩坑,必须**看板视图(定性)+ 透视表视图(定量)**双管齐下。

### 3a. 人力分布(定性 — 看卡片密度)
新建「**看板视图**」→ 命名 `人力分布` → 分组依据选「负责人」→ 每人一列,卡片堆叠密度即负载,一眼看出谁列满、谁是空列。
可开二级分组按「状态」细分每人活在哪个阶段。

### 3b. 人力负载表(定量 — 可排序找超载)★核心
新建「**透视表视图**」→ 命名 `人力负载`:
- **行**:负责人
- **值**:任务数(COUNT)、预计工时(SUM)、实际工时(SUM)
- **筛选**:状态 ≠ 已上线(只看在途任务)

按「预计工时 SUM」列排序 → **数字最大 = 当前最超载的人**;任务数为 0 或工时低 = 有余量。

### 3c. 单点风险识别(可选但推荐)
再建一个透视表 → **行**:负责模块 → **值**:负责人(去重 COUNT)→ 某模块下 COUNT = 1 即**单点风险**,需安排备份。

---

## 第四步:手机快速录入(iPhone)

> 诚实说:钉钉手机录入真实 **6-8 步、15-30 秒**,没有"秒记",无一键直达表单的桌面/锁屏捷径。靠下面三招压到最短。

1. **一次性准备**:在该表新建「**表单视图**」,只露 4 个字段——需求标题 + 状态 + 优先级 + 负责人(工时/进度/备注一律回 PC 补)。把这张表在「文档」里「收藏」并置顶。
2. **日常录入**:钉钉 App → 文档 → 置顶的表 → 底部切「表单视图」→ 填标题(**点语音录入**,念一句"XX接口联调 P0 张三 待评审")→ 提交。自动入库,看板即时更新。
3. **再提速**:把表单视图生成分享链接,存到 iPhone 备忘录或加到主屏,点开即表单,省掉进文档列表那两步。
4. **极速兜底**:路上只来得及记一句 → 微信「文件传输助手」发自己(5 秒),回 PC 再补进看板。这条进不了看板,只当临时收件箱,需定期清空。

---

## 第五步:每周固定节奏(共 30-40 分钟)

| 时间 | 动作 | 时长 |
|---|---|---|
| **周一 · 规划** | 开「任务进度」看板,梳理本周推进的需求,定负责人/优先级;把新需求从待评审推到排期中 | 15 min |
| **周三 · 负载校准** | 开「人力负载」透视表,看谁预计工时 SUM 最大(超载)、谁为 0(有余量),超载的活匀给有余量的人;扫一眼单点风险透视表 | 10 min |
| **周五 · 复盘+数据维护** | 更新本周完成需求的「实际工时」,对比预计识别估时偏差;已上线的推进状态(自动从在途筛选消失)。**这步是人力看板数字准确的命脉** | 15 min |

> 持续动作:手机用表单视图随时录入新需求,别让它只存在脑子里或聊天记录里。

---

## ⚠️ 必须知道的 4 个边界

1. **人力是"统计型",不是"产能型"**
   透视表告诉你"谁手上几条 / 累计工时大",告诉不了你"谁这周还剩多少工时"。要精确产能规划,自己加「周容量」字段用公式算。

2. **数字准不准,全靠你填工时**
   Garbage in garbage out。不填预计/实际工时,透视表就是空的。周五固定 10-15 分钟维护,否则数据腐烂。

3. **手机"秒记"不现实**
   真实 15-30 秒、6-8 步,别期待 Apple 提醒事项那种 2 秒体验。靠表单视图 + 置顶 + 语音录入缓解。

4. **数据锁在钉钉 + 政策风险**
   钉钉有把功能在免费/付费间挪动的历史;免费版自动化 500 次/月、高级 AI 字段/权限锁企业版(¥18/人/月)。透视表 2026.3 才上线(才几个月),有早期 bug 风险,建好后实测筛选/汇总。**重要数据定期导出 Excel 备份到本地。**

---

## 备选(若不选钉钉)

- **更看重微信秒记、能接受"定性"负载(看不出工时数字)** → **腾讯文档智能表**:iPhone 微信小程序 + 收集表 + 文件传输助手置顶是国内最顺的快记入口。硬伤:人力负载定量看板做不到(个人版无仪表盘/透视表,官方明示透视表不支持引用智能表数据),只能按负责人分组看卡片数估算。
- **想要最强看板、不介意装飞书** → **飞书多维表格**:多视图最全,任务+人力都强,免费够用。代价:装飞书全家桶,且你没用过。

---

*判定优先级:能同时做好"任务看板 + 人力看板" 且 手机快记 且 国内 Win+iOS 双端顺 > 不装新软件 > 其他。按此,钉钉智能表是综合最优。*
